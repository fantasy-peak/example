# xmake-example
```
1. bash <(curl -fsSL https://xmake.io/shget.text)
2. git clone https://github.com/fantasy-peak/xmake-example.git
3. cd xmake-example && mkdir build && cd build
4. cmake ..
5. make -j8
```